; sliders
(defpoll slider-volume :interval "1s" "examples/slider.sh")

(defwidget slider-vol []
  (box :class "slider-vol" 
       :orientation "h" 
       :space-evenly "false"
    (button :onclick "notify-send heyo" "")
    (scale :min 0
           :max 101
           :value slider-volume
           :onchange "amixer -D pulse sset Master {}%"
    )
  )
)
