(defpoll time	:interval "1s" "date '+%I:%M %p'")

(defwidget time []
  (box :class "time"
       :space-evenly false
      (button :onclick "notify-send test" "")
      (label :text "${time}")
  )
)
